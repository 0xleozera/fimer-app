import React from 'react';
import { Text } from 'react-native';

import { BaseScreen } from 'components';

const Home = () => (
  <BaseScreen>
    <Text>Home</Text>
  </BaseScreen>
);

export default Home;
